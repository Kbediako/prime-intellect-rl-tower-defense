# Prime Intellect authentication (do not commit real keys)
PRIME_API_KEY=
# Optional: set when using team-scoped resources
PRIME_TEAM_ID=
# Optional override for Prime Inference base URL
PRIME_INFERENCE_BASE_URL=https://api.pinference.ai/api/v1
# Example env var used by the training config (rename as needed)
CUSTOM_API_KEY=
